/** The one mid-level heading on the site: sits between the 30/36px h1 and the
 *  17px body. Carries type only — callers add their own bottom margin. */
export const H2 = "text-2xl font-semibold text-zinc-950 dark:text-zinc-100";
